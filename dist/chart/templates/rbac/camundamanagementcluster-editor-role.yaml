{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "camunda-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "camunda-operator.resourceName" (dict "suffix" "camundamanagementcluster-editor-role" "context" $) }}
rules:
- apiGroups:
  - core.camunda.io
  resources:
  - camundamanagementclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.camunda.io
  resources:
  - camundamanagementclusters/status
  verbs:
  - get
{{- end }}

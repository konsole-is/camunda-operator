---
feature: management-cluster
spec: docs/superpowers/specs/2026-08-23-management-cluster-design.md
plan: docs/superpowers/plans/2026-08-23-management-cluster-plan.md
tracking_issue: #185
feature_branch: feat/management-cluster
feature_worktree: .claude/worktrees/management-cluster
sub_pr_approval: autonomous
sub_pr_review_loop: on
sub_pr_target: feature-branch
integration_pr: #215
status: review
---

# Management plane — orchestration state

The user granted full autonomy on 2026-08-23: file issues, write the plan, implement, review-loop every sub-PR until clean, self-merge, open the integration PR. The user is not available for questions; the orchestrator decides and records the decision here. The merge to main stays the user's.

## Phases

- **Wave 1 (foundational)** — `#186` API types and shared contracts
- **Wave 2 (foundational)** — `#187` Identity on oidc, contract output, cluster discovery and claim (depends on #186)
- **Wave 3 (consumers, parallel)** — `#188` Keycloak modes, `#189` Console + ping, `#190` Web Modeler + cluster list + user (depend on #187)
- **Wave 4 (parallel)** — `#191` e2e flows, `#192` docs, `#208` coherence sweep (depend on wave 3)
- **Wave 5 (convergence)** — `#213` Ready under ImmutableAfterStart, the last seams, the status table (from the final checkpoint; depends on #211)

## PRs / worktrees

| Issue | Branch | Worktree path | PR (→ base) | Status |
| --- | --- | --- | --- | --- |
| #186 | feat/management-cluster--api-types | .claude/worktrees/management-cluster/.claude/worktrees/api-types | #193 → feat/management-cluster | self-merged (64ccf29) |
| #187 | feat/management-cluster--identity-oidc-contract | .claude/worktrees/management-cluster/.claude/worktrees/identity-oidc-contract | #200 → feat/management-cluster | self-merged (b80fd1a) |
| #188 | feat/management-cluster--keycloak-modes | .claude/worktrees/management-cluster/.claude/worktrees/keycloak-modes | #205 → feat/management-cluster | self-merged (d10e55d) |
| #189 | feat/management-cluster--console-ping | .claude/worktrees/management-cluster/.claude/worktrees/console-ping | #203 → feat/management-cluster | self-merged (84ef8b8) |
| #190 | feat/management-cluster--web-modeler | .claude/worktrees/management-cluster/.claude/worktrees/web-modeler | #204 → feat/management-cluster | self-merged (f50c39e) |
| #191 | test/management-cluster--e2e-flows | .claude/worktrees/management-cluster/.claude/worktrees/e2e-flows | #211 → feat/management-cluster | self-merged (c079ce5) |
| #192 | docs/management-cluster--user-docs | .claude/worktrees/management-cluster/.claude/worktrees/user-docs | #209 → feat/management-cluster | self-merged (2fdeb6e) |
| #208 | refactor/management-cluster--coherence-sweep | .claude/worktrees/management-cluster/.claude/worktrees/coherence-sweep | #210 → feat/management-cluster | self-merged (833f300) |
| #213 | refactor/management-cluster--ready-and-seams | .claude/worktrees/management-cluster/.claude/worktrees/ready-and-seams | #214 → feat/management-cluster | self-merged (427b87f) |

## Contracts

| Name | Realization | Realized in | Status |
| --- | --- | --- | --- |
| `management-api-types` | pre-merge stub PR | #193 (64ccf29) | locked |
| `platform-oidc-management` | pre-merge stub PR | #193 (64ccf29) | locked |
| `platform-images` | pre-merge stub PR | #193 (64ccf29) | locked |
| `cluster-gateway-status` | pre-merge stub PR | #193 (64ccf29) | locked |
| `keycloak-cr-types` | pre-merge stub PR | #193 (64ccf29) | locked |
| `management-render-core` | pre-merge stub PR | #200 (b80fd1a) | locked |
| `management-controller-core` | pre-merge stub PR | #200 (b80fd1a) | locked |
| `ping-and-list-env` | data-only | plan table, proven by #211 (c079ce5) | locked |
| `e2e-flow-names` | data-only | #211 (c079ce5) | locked |

## Bubble-up log

- 2026-08-23 — The single Optimize redirect URI, on the user's question. Mechanics: all CamundaOptimize instances share the realm's one `optimize` client (the contract carries one id and secret); Management Identity's preset takes one root URL (`spec.optimize.externalUrl`) and rewrites the client on every start, so a hand-added callback is wiped on every Identity roll — the docs recommended exactly that hand edit; fixed in 27e5722 (the page now says the edit does not survive and points at the `oidc` mode for more than one Optimize). The SaaS operator avoids the problem with Auth0, not with a Keycloak trick. The durable fix (CamundaOptimize.spec.externalUrl + the plane owning the redirect-URI list) was a named-but-unfiled follow-up from #205; filed as #223 and linked to the epic.
- 2026-08-23 — `spec.identityProvider.keycloak.scheduling` added on the user's question ("is that a limitation or did it just get missed?" — it was missed: the design sketch never carried it and no exclusion was recorded, while every sibling workload takes the block). Shipped as a78ec40 (+7422ab2 import fix): the same `SchedulingSpec` (nodeAffinity, podAffinity, tolerations) mapped to the Keycloak custom resource's first-class `spec.scheduling` (the wrapper declares affinity+tolerations of it), unit test for the mapping and the absence when unset, the schema round-trip writes it, CRD page documents it. `podLabels`/`podAnnotations`/`extraEnv` stay off the managed Keycloak on purpose (env is Identity-bootstrap-critical; the pod template already carries the discovery labels). The a4bc114 review round posted one finding (a failed pre-check skips the selector-driven cleanup) → filed #222 with the design, reasoning on the thread; #221 and #222 linked under the epic (all follow-ups are).
- 2026-08-23 — `spec.namespaceSelector` added on the user's request during the integration review (a4bc114): a LabelSelector over Namespace labels that narrows `clusterSelector`; unset or `{}` puts no bound (the admission-webhook convention — unset must not mean "no namespaces", or every existing spec would select nothing), a bound that matches no namespace admits none. The controller lists matching namespaces metadata-only per reconcile, watches Namespaces metadata-only, and `releaseClaims` treats a namespace leaving the bound like a cluster leaving `clusterSelector` (user and ping first, claim last). Round 6 of the review also landed: the owner-checked contract delete carries a UID precondition, and a failed ping apply reports `WriteFailed` on the row (109729a); #221 filed (basic→OIDC transition loses the credential the user removal needs); the `claimName` `=` case went to #219; the Failing/Degraded/Down docs claim was rejected again with the verified ocf reasoning.
- 2026-08-23 — #215 rounds 4 and 5. Round 4: the claim was released before the Web Modeler user was withdrawn, on deselect and at finalization, so another plane could adopt the user this one was removing; fixed in aadf9fc (`releaseClaims` runs after the user and ping withdrawals and only when both succeeded; the finalizer removes users first; a spec holds the removal and expects the claim kept), and the finalizer event says the removal was tried. Round 5 posted nothing on the changed code and suppressed seven findings in unchanged code — the stop signal; loop ended. Filed: #219 (spec validation: email with Web Modeler in Keycloak modes, query/fragment in the Keycloak URLs, the realm charset), #220 (a valueFrom ping entry cannot be replaced by the apply); #218 updated with the duplicate-grant-on-retry aggravation; the OIDC issuer repeat was already #217.
- 2026-08-23 — #215 round 3 (19:03Z): two posted race findings, both right, fixed in bf414fe with specs: the claim is applied under a per-management-cluster field manager (`AttachmentFieldManager(mc)`, the UID in the name) without forced ownership, so a second claimant gets a 409 and the row reads NotReady until the next pass reads the holder; a contract that does not exist is created (atomic), one that another owner holds is left alone with an error, force-apply only when owned. Suppressed: a refused user API was never called again without an event → the reconcile requeues after `RetryInterval` (30 s, `defaultRetryInterval`; the envtest suite sets 1 s) while a row reads `BasicAuthUserFailed`, spec proven to fail without it; the `applied` marker never re-validated → #218. A fourth round was requested past the cap because bf414fe is substantive code on the reviewed paths; the `Tests` check had one unrelated flake (`backupschedule` status read before the flush), fixed on the branch as 0f8dc4b so it reaches main.
- 2026-08-23 — Integration PR #215 review loop (the user asked for it and then: "go with your gut on those decisions, file any follow-up work yourself"). Round 1 (117 of 153 files read): one false positive (`new(expr)`, valid Go 1.26), rejected. Rounds 2 and 3 (18:28Z and 18:41Z reviews; a verifier subagent read each finding against the code): applied on the branch with specs — a failed Web Modeler user removal now keeps the Secret and retries on the sync path, best effort only at finalization (b0ec316); a renamed `spec.managementAuthConfigName` withdraws the old contract once the new one is written and the finalizer withdraws both names (085d4a9); `KeycloakOperatorNotInstalled` says to restart the operator; the conditions GoDoc names `MirroredSecretsReady`; the `identity.admin` GoDoc says only the oidc claim reports `ImmutableAfterStart` (0bced7a); the CRD page says `web-modeler` is reserved on attached basic-auth clusters and what a contract rename does to a CamundaOptimize. Rejected with reasons on the threads: the 26.7.0 validation ceiling (recorded decision, #212); the `AlreadyExists` adoption of a pre-existing `web-modeler` user (design decision: fixed name, adoption completes the crash window and rotation by deletion). Filed: #216 (record the initial claim from the first started Identity pod, not readiness) and #217 (attach an OIDC cluster only when its issuer matches the management plane's). Every thread replied and resolved; suppressed dispositions in a PR comment.
- 2026-08-23 — #211 merged with an un-triaged Copilot review (5002800925, 16:06Z on b6df959): the CI-fit push plus the request still attached from the round-3 remove/re-add produced a fourth review before the merge; the user pointed at it. Its three findings were right and land in #214 at 3f3983b (the two required Keycloak CRDs fail the install when a release lacks them, the flow records the operator install before it runs, the CRD page says no Secret exists for Identity's own client); the #211 threads carry replies and the suppressed disposition. Merge precondition from now on: list the Copilot reviews once more right before `gh pr merge` and triage anything newer than the last triaged review.
- 2026-08-23 — User instruction: run the e2e suite through CI only, never locally (it thrashed the laptop; the Claude Code process restarted once mid-work, and a local flow failed on resource pressure). The first CI run of the suite with the management flows (#211 at 07961a6) failed three ways on the runner's four vCPUs: the Keycloak Operator's standing 300m request (installed in BeforeSuite) pushed the Optimize flow's broker into `Insufficient cpu`; the management flows reserved about four CPUs at their peak (sized for the laptop); the suite passed Ginkgo's default one-hour suite timeout, which the Makefile never raised (only go test's). Fixed in b6df959: the suite installs the Keycloak CRDs alone before the manager (its startup probe needs the kind), the management-keycloak flow installs the operator in its namespace for the time of the flow and removes it with the namespace, the flows reserve ~2.5 CPUs at peak with a comment naming the budget, and `-ginkgo.timeout $(E2E_TIMEOUT)` joins the Makefile. The `Camunda 8.9` check is the evidence from here on.
- 2026-08-23 — Final checkpoint (`reviewing-feature-progress`, run by a reviewer subagent against #211's head 13a86a3 as the union): contracts realized, state file consistent, acceptance criteria covered except two #191 evidence items, 27 align-now findings. Routed: into #211 (its own fixes' tests: the Keycloak schema round-trip writes `spec.proxy.headers`/`db.url`/`db.schema`, the envtest asserts the proxy headers, the Identity client secret test runs both modes; the spec's rotation list and cross-namespace sentence; the guide and field reference point at the 26.7 ceiling; flow A re-run at the final head and a renovate dry run for the five new markers), into #213 (the `Ready=True` beside `IdentityReady=False/ImmutableAfterStart` defect, one `CAMUNDA_IDENTITY_TYPE` declaration, the Identity profile/type constants reunited, the precheck resolve-once call, the duplicated test helper, `PusherAppIDKey` and `camundaadmin.AssignRole` dead, `ClustersEnv` unexported, the cluster-user component value into `names.go`, the two no-`Suspend` builders explained, the prefix/heading sweep, the status table's unreachable `Failing`/`Degraded`/`Down` rows and the remaining mechanism sentences on four pages), and the epic body (Risks named a `KEYCLOAK_CLIENTS_<n>_*` candidate that never shipped; decision comment + body edit done). Deliberate, not changed: the nil-returning Keycloak builder when the kind is not served (nothing to delete), the 27.0.0 validation ceiling (#212), `keycloakKindServed` without `IsNoMatchError` (repo-wide shape, three precedents), the `keycloakEnv*` prefix on Identity's Keycloak-mode variables (the file header scopes it). Deferred to #176: `observedGeneration` wording from the template on eight pages; the same unreachable status rows on camundaoptimize.md and camundacluster.md. The closed sub-issues #187-#190 carry five pre-decision statements (documented decisions all); left as closed records.
- 2026-08-23 — Second root cause under the first: with Keycloak 26.6.4 Management Identity still failed, "Unable to connect to Keycloak" ×6. Live experiment on the fresh kind cluster: a standalone Identity pod built from the rendered Deployment minus `IDENTITY_CLIENT_SECRET` signs in as `KEYCLOAK_SETUP_USER` in master, creates the realm, PUTs the built-in clients (204, `realm-management` included on 26.6.4), creates `camunda-identity`, regenerates that client's secret itself (`POST .../clients/<id>/client-secret`), signs in with it, and builds every preset; the Optimize client gets the operator's `KEYCLOAK_INIT_OPTIMIZE_SECRET`. With the variable set, Identity tries client credentials first and never runs setup (the 8.9 chart sets no `IDENTITY_CLIENT_SECRET` either). Decision: the Keycloak modes render `IDENTITY_CLIENT_ID` only; `Clients.Identity` carries no `SecretRef` there; the generated `<name>-identity-client` Secret goes (nothing else read it: the contract publishes the Optimize client only). Lands in #191 with its own commit, goldens, and unit test, because #188 is merged and #210 is review-clean; #191 edits the three doc passages that named the Secret (`scratchpad/docs-identity-client.py`) after it merges #209 forward, so the behaviour change and its docs ship together. The previous session's `IDENTITY_CLIENT_SECRET` experiment (session 4e17a1ff, 09:20) was this same diagnosis cut short by the crash.
- 2026-08-23 — Resume after an IDE crash (session 012QtvcueQcN6WYrufnvgYrD). All three wave-4 worktrees held unpushed commits and uncommitted edits; committed and pushed as found (#209: quality pass + round-1 fixes; #210: audience fix, user-Secret convergence, GoDoc/dead-field sweep). Root cause of the #191 stall: Management Identity 8.9.8 never starts against Keycloak 26.7.x. Keycloak 26.7.0 ships the CVE-2026-9796 fix (keycloak/keycloak#49624), which returns 403 on every update of the `realm-management` client, verified live with the bootstrap admin and a fresh permanent admin; Identity's realm setup PUTs every client and dies there, and later restarts fail on client credentials (401) because the realm exists but the `camunda-identity` client never got its secret. 26.6.4 does not carry the fix and is what Camunda's own 8.9 load tests run. Decisions: `KEYCLOAK_VERSION=26.6.4` in the 8.9 matrix with a renovate ceiling `<26.7.0` (own rule, comment names the CVE); the suite installs the Keycloak Operator of the Keycloak it runs (`KEYCLOAK_OPERATOR_VERSION` > `KEYCLOAK_VERSION` > vendored CRD version) and applies only the CRD files the release publishes (the two client kinds arrived in 26.7.0); #209 documents the ceiling on both Keycloak-mode pages, the guide, and the installation prerequisite, and moves the examples to 26.6.4; no validation ceiling in the API (Camunda will patch Identity; follow-up issue to file at the integration PR). The previous session's realm-delete experiment on the kind cluster was part of this diagnosis; the cluster was recreated.
- 2026-08-23 — Wave-4 reviews. #209 (docs): fact-check found four remedies a reader would act on wrongly (ImmutableAfterStart needs the `camunda.io/identity-initial-claim` annotation removed; `status.gateway` is unset only while suspended; ping entries exist only with `spec.console`; Keycloak tag is `quay-optimized-<version>`) plus status-table gaps (`Suspending`/`PendingSuspension`; `KeycloakReady` never `Scaling`/`Degraded`) and an invalid platform-config example; quality pass locked the terms (`CamundaManagementCluster` for the kind, "management plane" for the notion) and moved conditions before commands; all in one fix round. #210 (sweep): the Keycloak-mode Web Modeler audiences were inverted (public audience empty) — the new realistic golden had pinned the wrong output and #191 hit it live in kind; fixed in #210 with a unit test (#191 drops its duplicate on merge-forward); the Web Modeler user withdrawal now converges from the labelled user Secrets (published before the user is created, `applied` after the grants) instead of per-cluster live reads; GoDoc `ConsoleReady`/`WebModelerReady` say `Disabled`, not absent; dead `GeneratedSecrets.WebModelerPusher` removed. #191 (e2e): Keycloak Operator installs and the CR reaches Ready in kind; Identity/Console/websockets Ready; the restapi needed the audience fix; the Keycloak `proxy-headers` moves to the first-class `spec.proxy.headers` (#191 commit).
- 2026-08-23 — Wave-3 checkpoint (union at d10e55d): READY FOR WAVE 4. Follow-ups filed as #208 (coherence sweep: Web Modeler user withdrawn on deselect/disable, Keycloak-mode goldens with Console/Web Modeler, helper/probe/gate unification, `alwaysReady` removed, GoDoc corrections, spec reconciled with seven doc-visible deltas); #191 told to install the Keycloak Operator before `deployManager()`, pin its version, add the management images to the matrix; #192 told the seven spec deltas and the status reasons. No late Copilot review on #205.
- 2026-08-23 — #205 merged at d10e55d with its Copilot loop unfinished: round 1 (7 posted + 13 suppressed) was fully triaged and applied; two consecutive 30-minute waits after remove/re-add produced no second review while a request stayed attached. Merged on the strength of the spec pass, the full quality pass, and a delta quality pass on the merge-forward + round 2 + polish (all clean). A late Copilot review on the closed PR is triaged at the wave-3 checkpoint / integration PR. Wave 3 complete: #203 (84ef8b8), #204 (f50c39e), #205 (d10e55d); #188/#189/#190 closed.
- 2026-08-23 — #204 fix round found that a nil-returning component builder leaves resources running when the user removes the spec block (`spec.webModeler` unset → both Deployments stayed). Convention added to the plan: optional components are always built and feature-gated off; ocf deletes gated-off resources and a Disabled component stays out of `Ready`. Propagated: #204 converts Web Modeler and Console (after merging #203 forward); #205 told by message to convert Keycloak (mode switch deletes the CR). Merge order for wave 3: #203 (done, 84ef8b8) → #204 → #205, each merging the feature branch forward (never rebase) before its own merge.
- 2026-08-23 — #205 (Keycloak modes) decisions: Identity re-applies the whole client representation on every start (verified in Identity source, camunda/camunda#59963), so redirect URIs can change across restarts; but `CamundaOptimize` has no `externalUrl` field, so the Optimize client root URL cannot be discovered. `spec.optimize.externalUrl` is added and required in the Keycloak modes (the contract always publishes the Optimize client; its Keycloak client needs a root URL), forbidden in oidc. Follow-up (not this epic): `CamundaOptimize.spec.externalUrl` + discovery by the management cluster, then the field becomes optional. Console is bootstrapped through the `KEYCLOAK_INIT_CONSOLE_*` preset (chart parity), not `KEYCLOAK_CLIENTS_<n>_*`; `KEYCLOAK_SETUP_REALM`/`CLIENT_ID` rendered with the documented defaults, no new field. `identity.admin` gains `email`; `firstName`/`lastName` were NOT added (no Identity docs page documents the env var; follow-up if Web Modeler needs a name on the first user). #203 (Console) decisions: ping endpoint = Console's in-cluster Service URL (spec corrected on the branch); 8.10 hub ping needs M2M credentials (unfiled follow-up); `ForceOwnership` on the four ping entries kept and documented on `CamundaClusterSpec.ExtraEnv`. #204 (Web Modeler): `BASIC` carries no credentials (the Web Modeler docs: the person enters them in the UI), the dedicated user is still created with the documented authorizations; `URL_GRPC` gets `grpc://`; pusher app id is the fixed identifier `web-modeler`; `SERVER_HTTPS_ONLY`/`PUSHER_APP_PATH` recorded in the spec.
- 2026-08-23 — Wave-2 checkpoint (union of #193+#200 at b80fd1a): not blocking. Decisions: the ping entries get their own field manager `camunda-operator/camundamanagementcluster-ping` (claim and ping under one manager would strip each other's fields and loop); wave-3 rules added to the plan Conventions (app-prefixed helpers, own `MirrorPurpose` constants, own golden fixture dirs, branches in `workload`/`replicas`/`componentEnv`/`resolveDatabases`/`finalize`/`attach`, hooks after `recordInitialClaim`, per-component hash scoping); `mirroredSecretComponent` moves into the builder list with its name in `names.go` as part of #188; Identity readiness `/actuator/health` (chart parity) and the `MirroredSecretsReady` condition recorded in the spec as shipped. Known limitations logged: `ownedBy` compares the bounded owner name (two >63-char same-prefix names in one namespace could both claim a contract); ping writes bump the cluster generation and roll its pods (expected — the env must reach the processes; #192 documents it).
- 2026-08-23 — #200 reviews. Decisions: `Ready` reflects a failed contract write (`Failed/WriteFailed` or `Conflict`) because the contract is what Optimize consumes; one cluster's claim conflict no longer stalls the plane (row reason, continue); the duplicated `WorkloadSpec` mutations move to a shared `pkg/workloadmutations` used by `camundaoptimize` and the management package (waves 3/4 use it too, never copy); the contract gets its own component value `management-auth`; `status.clusters` lists selected clusters only (issue #187 reconciled with a decision comment); the speculative names in `names.go` stay because they are the wave-3 contract; global `HashInputs` per component is a known limitation — wave-3 implementers scope hash inputs per component where a rotation must not roll every pod. Copilot's two "nil selector selects all" comments were wrong (`LabelSelectorAsSelector(nil)` is `Nothing()`), pinned by new specs.
- 2026-08-23 — Copilot on #193 flagged a confused-deputy risk: a namespaced kind selecting and patching CamundaClusters in every namespace. Decision: keep cross-namespace selection (one management plane serves a platform; creating the kind is a platform-administrator action, the RBAC boundary) but adopt LabelSelector semantics: unset selector = no cluster, `{}` = all. Propagated: spec Decision 6 and API section, plan Task 2.3 Step 0 (GoDoc fix + platform-admin note), #187 dispatch prompt. The #192 docs state it.
- 2026-08-23 — #193 Copilot loop reached the 3-round cap still producing "previously missed" findings on unchanged code. Decision: the correct round-3 items were applied in a final commit set (passwordSecretRef forbidden in oidc mode, repository-name pattern on `spec.images`, named `OIDCProviderType`, `hub-websockets` 8.10 default if the chart confirms it); the owner-label-with-namespace item was rejected (the contract carries `camunda.io/management-cluster-namespace` by design; #187 writes it). No round 4 requested; the orchestrator verified the final diff and merged. `reviewing-feature-progress` at the wave-2 boundary re-checks the api types.
- 2026-08-23 — Worktree subagents inherit the orchestrator's Bash isolation pin (`.claude/worktrees/management-cluster`) and cannot run commands in a sibling worktree. Decision: sub-worktrees live nested under the feature worktree at `.claude/worktrees/management-cluster/.claude/worktrees/<sub-name>` (`**/.claude/worktrees/` is gitignored). The #186 worktree was moved there; waves 2+ are created there. The PR-table paths below are the nested ones.
- 2026-08-23 — Copilot on #193: `MinLength=1` added on `IdentityAdminSpec.claimName/claimValue/username` (`has()` admits an explicit empty string). Pushed back on "GoDoc promises behaviour the scaffold does not implement": the kind's GoDoc is its contract and the controller lands on this feature branch before the integration PR.
- 2026-08-23 — Cluster discovery and the claim annotation moved from #189 (Console) into #187 (core) so that #189 and #190 can run in parallel against one attachment. Propagated: plan Task 2.3, issue bodies #187 and #189 updated.

## Pending snapshot

1. #215 at 7422ab2 (namespaceSelector, keycloak scheduling, rounds 1-7 of review all triaged; body reconciled twice): a review round for a78ec40+7422ab2 requested 2026-08-23 20:3xZ; CI runs on the head. Triage that round, reply, resolve; check for a late review right before anything else.
2. When that round is triaged and CI is green on the final head: the teardown commit on the feature branch (delete `docs/superpowers/plans/2026-08-23-management-cluster-plan.md` and this state file; the spec stays), push, report ready-to-merge with #215 and the follow-ups #212, #216-#223 (all linked under epic #185, which auto-closes on merge with the open follow-ups still linked — accepted by the user). The merge to main is the user's.

## Resume checklist

For a fresh Claude session resuming this work: invoke `feature-dev-workflow:resuming-a-feature` — it executes the steps below, routes by the `status:` frontmatter, and works the `## Pending snapshot`. Fallback if that skill is unavailable:

1. Read this state file in full.
2. Read the plan at the path in the `plan:` frontmatter.
3. Read the spec at the path in the `spec:` frontmatter.
4. Verify each open PR's actual state via `gh pr view <num>`.
5. For each `in-progress` or `draft` row, `cd` to the worktree path and check `git status -sb` — an `ahead` count in the header means unpushed commits, and no upstream in the header means the branch was never pushed at all.
6. Re-dispatch subagents as needed per `feature-dev-workflow:developing-a-feature` (parallel waves still in flight; the orchestrator watch loop continues), or work the `## Pending snapshot` when development is past.

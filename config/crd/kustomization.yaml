# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/core.camunda.io_camundaclusters.yaml
- bases/core.camunda.io_camundaplatformconfigs.yaml
- bases/core.camunda.io_camundaclusterpresets.yaml
- bases/core.camunda.io_elasticsearchclusters.yaml
- bases/core.camunda.io_elasticsearchclusterpresets.yaml
- bases/core.camunda.io_databases.yaml
- bases/core.camunda.io_databaseserverconfigs.yaml
- bases/core.camunda.io_databaseconfigs.yaml
- bases/core.camunda.io_secondarystorageconfigs.yaml
- bases/core.camunda.io_objectstorageconfigs.yaml
- bases/core.camunda.io_backupschedules.yaml
- bases/core.camunda.io_pointintimerestores.yaml
- bases/core.camunda.io_logicalrestores.yaml
- bases/core.camunda.io_camundaoptimizes.yaml
- bases/core.camunda.io_camundamanagementclusters.yaml
- bases/core.camunda.io_managementauthconfigs.yaml
- bases/core.camunda.io_pvcautoresizes.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
#configurations:
#- kustomizeconfig.yaml

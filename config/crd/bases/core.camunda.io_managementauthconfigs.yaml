---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: managementauthconfigs.core.camunda.io
spec:
  group: core.camunda.io
  names:
    kind: ManagementAuthConfig
    listKind: ManagementAuthConfigList
    plural: managementauthconfigs
    singular: managementauthconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ManagementAuthConfig is the contract CRD that carries the Management
          Identity OIDC configuration — endpoints, client credentials, and audience —
          for components that live outside the orchestration cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ManagementAuthConfig
            properties:
              audience:
                description: Audience expected in access tokens issued for this client.
                minLength: 1
                type: string
              authUrl:
                description: |-
                  AuthURL is the OIDC authorization endpoint used for browser login
                  redirects.
                type: string
                x-kubernetes-validations:
                - message: authUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
              baseUrl:
                description: BaseURL is the base URL of the Management Identity service.
                type: string
                x-kubernetes-validations:
                - message: baseUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
              clientId:
                description: ClientID is the default machine-to-machine client ID.
                minLength: 1
                type: string
              clientSecretRef:
                description: |-
                  ClientSecretRef names the Secret key holding the client secret for the
                  machine-to-machine client.
                properties:
                  key:
                    description: Key in the Secret holding the value.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the Secret holding the value.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret.
                    minLength: 1
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              issuerBackendUrl:
                description: |-
                  IssuerBackendURL is the issuer URL for in-cluster container-to-container
                  communication. Consumers default it to IssuerURL when empty.
                type: string
                x-kubernetes-validations:
                - message: issuerBackendUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
              issuerUrl:
                description: IssuerURL is the OIDC issuer URL used to validate tokens.
                type: string
                x-kubernetes-validations:
                - message: issuerUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
              jwksUrl:
                description: JwksURL is the JWKS endpoint used to fetch token signing
                  keys.
                type: string
                x-kubernetes-validations:
                - message: jwksUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
              tokenUrl:
                description: |-
                  TokenURL is the OIDC token endpoint used to acquire machine-to-machine
                  tokens.
                type: string
                x-kubernetes-validations:
                - message: tokenUrl must be a valid http or https URL
                  rule: isURL(self) && (url(self).getScheme() == 'http' || url(self).getScheme()
                    == 'https')
            required:
            - audience
            - authUrl
            - baseUrl
            - clientId
            - clientSecretRef
            - issuerUrl
            - jwksUrl
            - tokenUrl
            type: object
          status:
            description: status defines the observed state of ManagementAuthConfig
            properties:
              conditions:
                description: |-
                  Conditions represent the current validation state; the Ready condition
                  carries reasons Healthy or MissingSecret.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last generation reconciled
                  by the operator.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

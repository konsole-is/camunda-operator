apiVersion: core.camunda.io/v1
kind: CamundaRelease
metadata:
  labels:
    app.kubernetes.io/name: camunda-operator
    app.kubernetes.io/managed-by: kustomize
  name: camundarelease-sample
spec:
  # TODO(user): Add fields here

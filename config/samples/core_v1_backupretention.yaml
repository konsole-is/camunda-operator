apiVersion: core.camunda.io/v1
kind: BackupRetention
metadata:
  labels:
    app.kubernetes.io/name: camunda-operator
    app.kubernetes.io/managed-by: kustomize
  name: backupretention-sample
spec:
  # TODO(user): Add fields here
